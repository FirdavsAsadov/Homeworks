namespace N35_HT_Task2;

public struct Rectangle
{
    public static (int width, int height)
        Rectangles = (20,30);

    private int widths = Rectangles.width;
    private int heights = Rectangles.height;

    public Rectangle()
    {
    }
}